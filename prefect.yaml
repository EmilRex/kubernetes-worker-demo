# File for configuring project / deployment build, push and pull steps

# Generic metadata about this project
name: kubernetes-worker-demo
prefect-version: 2.10.6

# build section allows you to manage and build docker images
build:
- steps.get_current_sha: {}
- prefect_docker.projects.steps.build_docker_image:
    # TODO: https://github.com/PrefectHQ/prefect-docker/issues/46
    requires: git+https://github.com/EmilRex/prefect-docker.git
    image_name: "{{ prefect.variables.image_name }}"
    # TODO: https://github.com/PrefectHQ/prefect/issues/9346
    # tag: "{{ current_sha }}"
    tag: latest
    dockerfile: auto
    push: true
    platform: "linux/amd64"

# push section allows you to manage if and how this project is uploaded to remote locations
push: null

# pull section allows you to provide instructions for cloning this project in remote locations
pull:
- prefect.projects.steps.set_working_directory:
    directory: /opt/prefect/kubernetes-worker-demo
